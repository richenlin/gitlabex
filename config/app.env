# 服务器配置
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
SERVER_MODE=debug

# 数据库配置
DATABASE_URL=postgres://gitlabex:password123@localhost:5432/gitlabex?sslmode=disable
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=gitlabex
DATABASE_PASSWORD=password123
DATABASE_NAME=gitlabex

# Redis配置
REDIS_URL=redis://:password123@localhost:6379/0
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=password123
REDIS_DB=0

# GitLab配置（基础配置，OAuth配置来自oauth.env）
GITLAB_URL=http://localhost:8081
GITLAB_INTERNAL_URL=http://gitlab
GITLAB_TOKEN=your-gitlab-token
GITLAB_ROOT_PASSWORD=b75hZ0qcwLKD

# OnlyOffice配置
ONLYOFFICE_URL=http://localhost:8000
ONLYOFFICE_JWT_SECRET=gitlabex-jwt-secret-2024
ONLYOFFICE_CALLBACK_URL=http://localhost:8080/api/onlyoffice/callback
ONLYOFFICE_MAX_FILE_SIZE=52428800

# JWT配置
JWT_SECRET=gitlabex-app-jwt-secret-2024
JWT_EXPIRE_TIME=24h

# 日志配置
LOG_LEVEL=info
LOG_FORMAT=json
LOG_OUTPUT=stdout

# 应用配置
APP_NAME=GitLabEx Community
APP_VERSION=1.0.0
APP_ENV=development
APP_DEBUG=true

# 文件上传配置
UPLOAD_MAX_SIZE=52428800
UPLOAD_ALLOWED_TYPES=jpg,jpeg,png,gif,pdf,doc,docx,xls,xlsx,ppt,pptx,txt,md

# 邮件配置（可选）
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@gitlabex.com

# 监控配置（可选）
PROMETHEUS_ENABLED=false
PROMETHEUS_PORT=9090

# 前端配置
FRONTEND_URL=http://localhost:3000

# 安全配置
CORS_ORIGINS=http://localhost:3000,http://localhost:8000,http://localhost:8080,http://localhost:8081
RATE_LIMIT_ENABLED=true
RATE_LIMIT_RPS=100 